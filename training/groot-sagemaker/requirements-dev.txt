# GR00T-N1.6 SageMaker 로컬 개발 의존성
# 로컬 스크립트 실행에 필요한 패키지 (학습/추론 컨테이너 내부 패키지와 다름)
#
# 설치:
#   pip install -r requirements-dev.txt

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# SageMaker Python SDK (Training Job, Pipeline, Model Registry 등)
sagemaker>=2.220.0

# 설정 파일 파싱
pyyaml>=6.0

# HuggingFace 모델 다운로드 (data/download_model.py)
huggingface_hub>=0.23.0

# 진행률 표시 (선택)
tqdm>=4.66.0
