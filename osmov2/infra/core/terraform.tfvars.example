aws_region   = "ap-northeast-2"
project_name = "aws-osmo"
environment  = "dev-repro"

# Leaving availability_zones empty makes main.tf pick the region's G7e-capable
# AZs from its built-in g7e_azs_by_region map (Seoul resolves to
# ap-northeast-2a/2b). Set it explicitly only to override that pin. See the
# per-region examples (terraform.use1/use2/usw2.tfvars.example) for other
# regions. az_count sizes EKS + stateful services; karpenter_az_count sizes the
# GPU private subnets.
availability_zones = []
az_count           = 2
karpenter_az_count = 2

# The EKS API endpoint is private-only by default. Add narrow CIDRs only when
# your network path requires temporary public access.
cluster_endpoint_public_access_cidrs = []

# Use one NAT gateway for lower-cost reproducibility tests. Set false for
# AZ-aligned NAT in reference-ha deployments.
single_nat_gateway = true

eks_cluster_version = "1.35"

system_node_instance_types = ["m7i.2xlarge"]
system_node_min_size       = 3
system_node_desired_size   = 3
system_node_max_size       = 5

reference_ha        = false
deletion_protection = false
