.DS_Store

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

!infra/core/terraform.tfvars.example
!infra/fsx/terraform.tfvars.example
!infra/ingress/terraform.tfvars.example
!infra/observability/terraform.tfvars.example

# Local generated files
.osmo/
.nvidia
ngc-api-key*
*.log
tmp/

# Slide screenshots extracted from osmo-on-aws.zip. Reference material only —
# not ours to redistribute, so the decks draw their own diagrams instead.
docs/decks/assets/osmo-on-aws/

# Deck PDFs are rendered from the HTML sources on demand.
docs/decks/pdf/
