# IsaacLab feature/newton 브랜치 의존성
# Python 3.11 필수 (venv 사용 권장)
#
# 사용법:
#   python3.11 -m venv ~/venv311
#   source ~/venv311/bin/activate
#   pip install -r requirements_newton.txt --extra-index-url https://pypi.nvidia.com
#   pip install --no-deps -e source/isaaclab
#   pip install --no-deps -e source/isaaclab_assets
#   pip install --no-deps -e source/isaaclab_tasks
#   pip install --no-deps -e source/isaaclab_tasks_experimental
#   pip install --no-deps -e source/isaaclab_experimental
#   pip install --no-deps -e source/isaaclab_newton
#   pip install -e "source/isaaclab_rl[skrl]"

# === 빌드 도구 (다른 패키지보다 먼저 설치되어야 함) ===
setuptools

# === Isaac Sim ===
isaacsim[all,extscache]==5.1.0

# === warp-lang (NVIDIA dev index 필요: --extra-index-url https://pypi.nvidia.com) ===
warp-lang==1.11.0.dev20251205

# === Newton 물리 엔진 (PyPI 버전 아님, GitHub 특정 태그) ===
newton @ git+https://github.com/newton-physics/newton.git@beta-0.2.1
mujoco-warp @ git+https://github.com/google-deepmind/mujoco_warp.git@e9a67538f2c14486121635074c5a5fd6ca55fa83
mujoco>=3.4.0

# === isaaclab 핵심 의존성 (--no-deps로 설치하므로 여기서 보충) ===
onnx==1.16.1
prettytable==3.3.0
tqdm==4.67.1
hidapi==0.14.0.post2
gymnasium==1.2.0
pyglet>=2.1.6
transformers
einops
fast_simplification

# === RL 프레임워크 ===
skrl>=1.4.2,<2.0

